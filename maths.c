#include <stdio.h>

int main ()
{
	int x = 20, c = 10;

	printf(" %2i + %2i = %2i \n", x, c, x + c );
	printf(" %2i - %2i = %2i \n", x, c, x - c );
	printf(" %2i x %2i = %2i \n", x, c, x * c );
	printf(" %2i / %2i = %2i \n", x, c, x / c );

	return 0;

}
